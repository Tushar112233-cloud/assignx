"use client";

import { PortalPage } from "@/components/portals";

/**
 * Campus Connect / Portal Page
 * Multi-role portal system: Student (Campus Connect), Professional (Job Portal), Business (VC/Funding)
 * Shows tab switcher for multi-role users, single portal for single-role users
 */
export default function CampusConnectRoute() {
  return <PortalPage />;
}
